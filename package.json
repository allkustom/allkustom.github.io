{
  "type": "module",
  "scripts": {
    "optimize-media": "node scripts/optimize-thumbnails.mjs",
    "build": "npm run optimize-media && eleventy --pathprefix=allkustom.github.io",
    "serve": "npm run optimize-media && eleventy --serve --input=source"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "markdown-it": "^14.1.0"
  }
}
